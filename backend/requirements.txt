# Django Backend Dependencies
Django==5.1.3
channels>=4.0.0  # Django Channels for WebSocket
daphne>=4.0.0  # ASGI server for Channels
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.0
drf-spectacular==0.27.0
django-cors-headers==4.6.0
django-redis==5.4.0
django-filter==24.3
django-ratelimit==4.1.0
django-otp==1.3.0
django-csp>=4.0  # Content Security Policy headers
psycopg2-binary>=2.9.0  # PostgreSQL adapter
argon2-cffi==23.1.0
cryptography>=41.0.0  # Required by PyJWT for RS256 JWT signing (HS256 fallback works without it)
gunicorn==23.0.0
python-decouple==3.8
dj-database-url==2.2.0
Pillow==10.4.0
celery==5.3.6
kombu==5.3.4  # Celery messaging library
celery-progress==0.3  # Progress tracking for Celery tasks
openpyxl==3.1.5
xlrd==2.0.1

# FastAPI Timetable Service Dependencies - Hardware Adaptive
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12
ortools==9.14.6206
python-constraint==1.4.0
numpy>=1.24.0
networkx==3.2.1
python-louvain==0.16
scikit-learn>=1.3.0
websockets==12.0
torch>=2.6.0

# Hardware-Adaptive System Extensions
scipy>=1.11.0  # Use prebuilt wheel (no compiler needed)
deap>=1.4.0  # Genetic Algorithm (prebuilt wheel)
aiohttp>=3.9.0  # Async HTTP client (prebuilt wheel)
py-cpuinfo==9.0.0  # CPU detection
joblib==1.3.2  # Parallel processing
multiprocess==0.70.15  # Process isolation
python-socketio==5.10.0  # WebSocket support
cloudpickle==3.0.0  # Serialization
structlog==23.2.0  # Structured logging
rich==13.7.0  # Terminal output
tqdm==4.66.1  # Progress bars

# GPU Acceleration (Optional - auto-detected)
# torchvision>=0.15.0; sys_platform != "darwin"
# torchaudio>=2.0.0; sys_platform != "darwin"
# pyopencl>=2023.1; platform_machine != "arm64"  # AMD/Intel GPU
# pynvml>=11.5.0  # NVIDIA monitoring

# Cloud SDKs (Optional - auto-detected)
# google-cloud-compute>=1.15.0  # Google Cloud
# azure-mgmt-compute>=30.0.0  # Azure

# Mathematical optimization
# cvxpy>=1.4.0  # Convex optimization (optional)
pulp>=2.7.0  # Linear programming

# Time series
pandas>=2.1.0  # Use prebuilt wheel
pytz>=2023.0

# Shared Dependencies
redis==5.2.0
httpx==0.27.2
requests==2.32.3
python-dotenv==1.0.1
sentry-sdk[fastapi]==2.19.2
python-jose[cryptography]==3.3.0
setuptools>=65.0.0
psutil==6.1.0  # System resource monitoring for adaptive parallelism
# gevent==23.9.1  # SKIP: Requires C++ compiler (optional)
# eventlet==0.33.3  # SKIP: Requires C++ compiler (optional)
cryptography>=41.0.0  # Security
bcrypt>=4.0.0  # Password hashing

# Phase 1: Real-Time Progress & WebSocket
websockets==12.0  # Already present, keeping for clarity
redis[hiredis]==5.2.0  # High-performance Redis pub/sub

# Phase 2: Monitoring & Observability
prometheus-client==0.21.0  # Prometheus metrics
prometheus-fastapi-instrumentator==7.0.0  # FastAPI metrics
flower==2.0.1  # Celery monitoring (already for Celery)

# Phase 3: Object Storage & Security
boto3==1.35.76  # AWS S3 client
minio==7.2.11  # MinIO client (S3-compatible)
django-storages==1.14.4  # Django S3 integration

# Development Tools
bandit==1.7.5
black==23.12.1
flake8==6.1.0
isort==5.13.2
mypy==1.7.1  # Type checking
debugpy==1.8.0  # Remote debugging for Docker

# Performance profiling (Development)
py-spy==0.3.14
memory-profiler==0.61.0
# line-profiler==4.1.1  # SKIP: Requires C++ compiler (optional)

# Documentation (Development)
mkdocs==1.5.3
mkdocs-material==9.4.8
