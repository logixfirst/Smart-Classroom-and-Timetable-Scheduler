# ============================================================
# requirements-dev.txt
# Development and test-only dependencies for the backend.
#
# Industry practice (Google / Stripe / Shopify):
#   Production requirements live in requirements.txt.
#   Test/dev tooling lives here and is NEVER installed in production images.
#   CI installs: pip install -r requirements.txt -r requirements-dev.txt
#
# ============================================================

# ── FastAPI test stack ────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0          # async test support (FastAPI)
pytest-cov==5.0.0               # coverage reporting
httpx==0.27.2                   # async test client for FastAPI ASGI app
respx==0.21.1                   # httpx request mocking (Django client, FastAPI calls)
fakeredis[aioredis]==2.25.1     # in-memory Redis for async tests
anyio[trio]==4.6.2              # anyio backend for pytest-asyncio

# ── Django test stack ────────────────────────────────────────
pytest-django==4.9.0            # pytest integration for Django
model-bakery==1.19.5            # fast Django model fixture factory
freezegun==1.5.1                # freeze/travel time in tests
factory-boy==3.3.1              # additional fixture factory (used with model-bakery)
django-test-plus==2.2.3         # extra DRF test helpers (optional convenience)

# ── Shared test tooling ──────────────────────────────────────
pytest-xdist==3.6.1             # parallel test execution (-n auto)
pytest-randomly==3.15.0         # random test order (catches ordering bugs)
pytest-timeout==2.3.1           # per-test timeout guard (no infinite hangs)
pytest-mock==3.14.0             # unittest.mock integration for pytest
coverage[toml]==7.6.3           # coverage with pyproject.toml config
Faker==30.3.0                   # realistic fake data for fixtures

# ── Security / lint (run in CI pre-test stage) ──────────────
bandit==1.7.5                   # SAST security scanner
safety==3.2.7                   # third-party CVE scanner
# trivy is managed by Codacy CI and is not a pip-installable requirement

# ── Type checking ────────────────────────────────────────────
mypy==1.11.2
django-stubs[compatible-mypy]==5.1.0
djangorestframework-stubs==3.15.1
